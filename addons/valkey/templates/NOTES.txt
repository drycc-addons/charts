CHART NAME: {{ .Chart.Name }}
CHART VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

A ValkeyCluster named "{{ include "valkey.name" . }}" has been created in namespace "{{ .Release.Namespace }}".

This resource is reconciled by the cluster-scoped Valkey Operator. The operator and
its CRDs must already be installed on the cluster. This chart does not deploy them.

Watch the cluster reach a Ready state:

  kubectl get valkeycluster {{ include "valkey.name" . }} --namespace {{ .Release.Namespace }} -w

Inspect the managed pods:

  kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Topology: {{ .Values.shards }} shard group(s), {{ .Values.replicas }} replica(s) per shard, workloadType {{ .Values.workloadType }}.
{{- if not .Values.persistence.enabled }}

WARNING: persistence is disabled. Data will not survive pod rescheduling.
{{- end }}
